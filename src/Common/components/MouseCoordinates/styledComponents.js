import styled from '@emotion/styled';
import tw from 'tailwind.macro';

const RenderPropsContainer = styled.div`
    ${tw`flex flex-col justify-center items-center w-full bg-gray-400 py-2`}
`;

export {RenderPropsContainer};