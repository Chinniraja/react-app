import styled from '@emotion/styled';
import tw from 'tailwind.macro';

const StyledGridArea = styled.div`
    ${tw`border-2 border-red-400 text-center`};
    width: 500px;
    height: 500px;
`;

export {StyledGridArea};