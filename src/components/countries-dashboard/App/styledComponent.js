import styled from '@emotion/styled';
import tw from 'tailwind.macro';

const CountriesDashboardConatiner = styled.div`
${tw`px-4`}
`;

export {CountriesDashboardConatiner};

// background-color: ${props => props.color};
// color: ${props => props.color === '#fff' ? 'black' : '#fff'};
